
return ({
    onEnded: ({ game, setNode }) => {
        if (game.isFirstTime || game.isFirstTime === undefined) {
            game.isFirstTime = false;
            setNode('19812');
        } else {
            setNode('19808');
        }
    },
});
